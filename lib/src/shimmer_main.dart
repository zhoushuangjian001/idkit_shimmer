import 'package:flutter/material.dart';

class IDKitShimmer extends StatelessWidget {
  const IDKitShimmer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
